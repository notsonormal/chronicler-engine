---
trigger: always_on
---

# Rules

## Memory Bank

You must utilize the Memory Bank in `.ag-memory/` for operational state and the `docs/` folder for project facts.

Read: Check `.ag-memory/MEMORY.json`, `.ag-memory/TODO.md`, and relevant files in `docs/` at the start of tasks.

Write: Update `TODO.md` with progress, `SCRATCHPAD.md` for thoughts, and `MEMORY.json` for behavioral habits.

Clean Up: Regularly prune `TODO.md` and wipe `SCRATCHPAD.md` after task completion.