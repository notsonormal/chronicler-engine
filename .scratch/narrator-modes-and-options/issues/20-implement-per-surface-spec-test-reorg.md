# Task: Per-surface spec + test-file reorganization

Type: task
Status: resolved
Blocked by: 19

## Question

Ticket 19 (resolved 2026-09-12) decided the browser tier gets per-surface spec files: DOM-observed scenarios live in `docs/specs/browser_<feature>.md`, HTTP-observed scenarios stay in `<feature>.md` — one scenario = one observation surface = one spec file = one tag. This ticket lands that reorganization end-to-end: spec moves, test moves, retags, the convention text, and its mechanical enforcement.

### Scope

Refined 2026-09-12 during plan review (user decision): the decomposition goes further than ticket 19's Q1=A. Story-log interactions (16.1–16.4) are the story-log feature's browser surface; the slash palette (17.1–17.9) is its own surface; the residual chrome spec is named `browser_dashboard.md` (user choice over `browser_shell.md`).

1. **Rename + trim the chrome spec** — `git mv docs/specs/browser.md docs/specs/browser_dashboard.md`; H1 → `# Feature Spec: Browser Dashboard`; delete the 16.1–16.4 and 17.1–17.9 blocks (16.5–16.7 keep their ids). Update prose references: `docs/diataxis/reference/frontend/dashboard.md:82` (link + cite retarget to `browser_slash_menu.md` 31.1–31.9, plus the stale two→three slash-command fix), `docs/diataxis/reference/frontend/ui_design.md:10` (glob phrasing `browser_*.md`).
2. **Create the six surface specs** with house headers (`# Feature Spec: Browser <Name>` + `Endpoint: browser DOM.`): `browser_story_log.md` (30.1–30.4 from 16.1–16.4), `browser_slash_menu.md` (31.1–31.9 from 17.1–17.9), `browser_options.md` (26.1–26.3 from options.md 24.3/24.8/24.9), `browser_games.md` (27.1–27.3 from games.md 20.1–20.3), `browser_prompt_presets.md` (28.1 from 21.27), `browser_worlds.md` (29.1–29.2 from 18.1–18.2). Donors lose the moved blocks and any emptied section headers; stay-behind scenarios keep their ids; gaps tolerated.
3. **Move + retag test files** — `tests/browser/story_log.rs` (16.1–16.4 cut, ~lines 9–176), `slash_menu.rs` (17.x cut, ~329–663, carries `type_into_command`), `options.rs` (tail cut, ~1019–1232), `games.rs` (~665–810), `worlds.rs` (~812–891, carries `open_world_edit`), `prompt_presets.rs` (~893–994); residual `behaviour.rs` → `dashboard.rs` (16.5–16.7 + the stdout-tee exemption). 22 moved tests get new path + id; 3 dashboard tags get the new path. Register all modules in `mod.rs`; rewrite the `//!` headers; prune imports per file.
4. **Pin the convention** — rewrite STRATEGY.md "SCENARIO tags" with the per-surface rule + spec↔test file mirroring (exemption wording now points at `dashboard.rs`); add the surface-consistency rule to `validate_feature_spec.py` (`browser_*.md` ⇔ tagged only from `tests/browser/`; non-`browser_*` specs never tagged from `tests/browser/`) and update `TAG_EXEMPT_TESTS` to the `dashboard.rs` path; regenerate `tests/AGENTS.md`.
5. **Bookkeeping** — amend ticket 12's `## Answer` + the map's 12-line (they describe the old behaviour.rs placement); `python build.py validate-docs`; full gate green.

### Notes-for-the-session

- Validator facts: keys coverage on (spec path, scenario id); cross-file scenario-number collisions are intentional; scans whole directory trees; no spec-path hardcodes — the rename costs only tag comments.
- All browser helpers are shared via `tests/test_utils/` and re-exported by the binary root (`with_test_page`, `send_action`, `dismiss_text_check_if_present`, `wait_for_element_children`, `wait_for_status_ready`/`wait_for_status_generating`, `CONFIG_PATH`/`TEST_WORLD`/`TEST_PERSONA`) — cuts need zero helper surgery.
- `invariants.rs` stays untagged (named exemption; boundary confirmed in ticket 19, Q6=A).
- Historical records untouched: `docs/CHANGELOG.md`, `docs/plans/*`, and old ticket bodies keep the old ids/paths. Only ticket 12's `## Answer` + the map's 12-line get amended.
- Rename blast radius (live references, verified 2026-09-12): 19 tag comments in `behaviour.rs`, `dashboard.md:82`, `ui_design.md:10`, the two `//!` headers feeding autogenerated `tests/AGENTS.md`.

## Answer

Shipped 2026-09-12, with the scope refined in the planning session (recorded in the Scope section above): the decomposition went further than ticket 19's Q1=A — story-log interactions (16.1–16.4) are the story-log feature's browser surface, the slash palette (17.1–17.9) is its own surface, and the residual chrome spec is named `browser_dashboard.md` (user choice; "shell" survives only in its precise repo sense — the static chrome of `assets/index.html:46`, now scenario 16.5's subject).

**Specs (7 browser-surface files).** `browser.md` → `browser_dashboard.md` keeping only 16.5–16.7 (ids unchanged, gaps where the moved blocks left); new files with house headers: `browser_story_log.md` 30.1–30.4, `browser_slash_menu.md` 31.1–31.9, `browser_options.md` 26.1–26.3 (from options.md 24.3/24.8/24.9), `browser_games.md` 27.1–27.3 (from games.md 20.1–20.3), `browser_prompt_presets.md` 28.1 (from 21.27), `browser_worlds.md` 29.1–29.2 (from 18.1–18.2). Donors trimmed verbatim; emptied section headers removed ("Set lifetime" in options.md, "Editor mode flags" in prompt_presets.md); stay-behind ids keep gaps.

**Tests (7 browser files + mod.rs).** `behaviour.rs` split and renamed `dashboard.rs` (16.5–16.7 + the stdout-tee exemption): `story_log.rs` (4), `slash_menu.rs` (9, carries `type_into_command` + `COMMAND_INPUT_SELECTOR` — used only by 17.x), `games.rs` (3), `worlds.rs` (2, carries `open_world_edit`), `prompt_presets.rs` (1), `options.rs` (3, tail cut). 22 moved tests retagged new path + id; 3 dashboard tags retagged path-only. Binary test count preserved (27 = 26 behaviour + 1 invariants); all pass. One import surprise: story_log's line-151 `expect` was `Option::expect`, not the playwright assertion — import dropped.

**Convention + enforcement.** STRATEGY.md "SCENARIO tags" rewritten: per-surface rule + spec↔test mirroring (`tests/browser/<feature>.rs` ⇔ `browser_<feature>.md`; dashboard chrome in `dashboard.rs`), exemption wording repointed at `dashboard.rs`. `validate_feature_spec.py` gains `find_surface_violations` (`browser_*.md` ⇔ tagged only from `tests/browser/`, and never the reverse), a `surface mismatch(es)` summary segment, and its own violation block; `TAG_EXEMPT_TESTS` repointed. Spot-checked both directions (browser test tagged `story_log.md` + HTTP test tagged `browser_dashboard.md` → 4 surface mismatches, exit 1; reverted clean). One mid-flight miss the validator caught: 18.1/18.2 initially left in `browser_dashboard.md` — removed.

**Prose + bookkeeping.** `dashboard.md:82` retargeted to `browser_slash_menu.md` 31.1–31.9 with the stale two→three slash-command fix (`/options` joined in ticket 11); `ui_design.md:10` now cites the `docs/specs/browser_*.md` glob; `tests/AGENTS.md` regenerated (7 modules listed); ticket 12's `## Answer` + the map's 12-line amended; `docs/plans/*` and CHANGELOG untouched.

**Verification.** Full gate green: 1614 passed, 0 failed, 2 skipped; spec-coverage step 140 declared, 140 covered, 0 gaps / 0 orphans / 0 untagged / 0 surface mismatches, quarantine 86/86 (140, not the plan's 139 estimate — the pre-reorg count was also 140; nothing lost). Unblocks the last fog: Documentation → ticket 21.
