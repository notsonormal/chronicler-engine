# ADR-NNN: Title (imperative or declarative, e.g. "Use SQLite for Game State")

<!--
Copy this file to adr-NNN-title-kebab-case.md to start a new ADR.
Fill in every required section. Delete this HTML comment block before committing.

Required:
  - Title line: # ADR-NNN: <title>
  - **Date:** YYYY-MM-DD at the top
  - **Status:** Accepted | Superseded by ADR-XXX | Deprecated
  - ## Context (or ## Problem Statement)
  - ## Decision — include "### Why X over Y" subsections when alternatives were compared
  - ## Consequences with subsections:
      ### Positive
      ### Negative
      ### Trade-offs  (omit only if no non-trivial choices were made)

Optional:
  - ## Related ADRs — bullet list, omit when no relations
  - ## References — code/plan links, omit when not needed
  - ## History — append-only date-stamped one-liners for non-material updates

Forbidden (do not add these sections — they belong in plans or guardrails):
  - Files Changed / Modified Modules
  - Architecture Impact
  - Verification (test counts, coverage)
  - Compliance (MUST-style rules)
  - Inline v2/v3 Update headers (write a superseding ADR instead)
  - References to `docs/plans/*.md` (plans archive + delete; plans reference ADRs, not reverse)
-->

**Date:** YYYY-MM-DD
**Status:** Accepted

## Context

<!-- What problem are we solving? What forces are in play? What constraints apply?
What is the current state and why is it insufficient? This is the "why now" section.
May be titled "## Problem Statement" instead — both are accepted. -->

## Decision

<!-- The decision, stated as a clear imperative. Include the core structural choice
and the key reasons. Include a short code or diagram snippet ONLY if it clarifies
the decision's shape — not to specify implementation.

When alternatives were genuinely compared, record the reasoning here as subsections,
not in a separate "Alternatives Considered" section. Example:

### Why X over Y

- Chose X because ...
- Rejected Y because ...

This keeps alternatives next to the decision they inform.
-->

## Consequences

### Positive

- ...

### Negative

- ...

### Trade-offs

- Chose X over Y because ...

<!-- Optional sections below. Omit entirely if unused. -->

## Related ADRs

- [ADR-000: Example](./adr-000-example.md) — relationship description

## History

- **YYYY-MM-DD**: Non-material update description (append-only; never edit prior entries).
