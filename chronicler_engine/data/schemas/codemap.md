# chronicler_engine/data/schemas/

## Responsibility
JSON Schema definitions for validating game data files. Ensures worlds, characters, maps, and settings conform to expected structure before engine bootstrap.

## Validation
`scripts/validate_data.py` validates all data files against these schemas at build time.
