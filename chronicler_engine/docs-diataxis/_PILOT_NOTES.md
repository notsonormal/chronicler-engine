---
title: Pilot Notes — docs-diataxis/ writing pattern
---

# Pilot Notes

Findings from writing the three pilot docs that establish the writing pattern for `chronicler_engine/docs-diataxis/`. Short on purpose — this file graduates the open questions on the wayfinder map.

## 1. Mode declaration per doc

| Pilot doc                                      | Mode         | Why it fits                                                                                                                                                                                |
|------------------------------------------------|--------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `reference/data_layer.md`                      | Reference    | Pure schema description. Column tables, foreign-key relationships, index list. Reader problem: "what tables exist and what columns do they have?" — look-up.                                |
| `reference/game_flow.md`                       | Reference    | Phase sequence, status-phase table, retry flowchart, error contract. Reader problem: "when the engine is in state X, what happens next?" — look-up. The "why" content is split out.       |
| `explanation/two-state-channels.md`            | Explanation  | Discursive treatment of the tradeoff the dual-channel design encodes. Reader problem: "why are there two generation-state signals and what is each for?" — understand.                       |
| `explanation/architecture.md`                  | Explanation  | System context, building blocks, deployment topology, quality guarantees. Reader problem: "how is the engine structured and what does it promise?" — understand.                            |

None of the pilots emerged as Tutorial or How-to. See §7.

## 2. Front-matter decision + reasoning

**Verdict: keep YAML front-matter, but only `diataxis:` + `title:` (and `arc52:` on the architecture doc).**

Findings while writing:

- `diataxis:` is **load-bearing**. The folder name `reference/` vs `explanation/` already signals the mode by path, but a reader scanning a list of files (search results, an index, a flat export) needs the mode without parsing the path. The cost is one line; the benefit is grep-ability across the whole tree.
- `title:` is **load-bearing**. The H1 may diverge from the title the file is referred to by in conversation (e.g. the architecture doc's H1 is "Architecture Overview" but cross-references want the bare title). Mirroring the H1 in `title:` keeps both stable and lets a downstream index use the front-matter value without parsing markdown.
- `arc52:` is **load-bearing on the architecture doc only**. It tells a reader which selective arc42 subset a given overview covers. Not useful on other docs — they are not arc52-shaped.

Front-matter does **not** duplicate what the content signals, because the content signals by *section structure* (a Reference doc reads like a schema catalog; an Explanation doc reads as a discursive discussion) while the front-matter signals by *machine-readable key*. They answer different questions. If we later add a `validate_docs.py` check that asserts the declared mode matches an automated content check, the front-matter becomes the canonical answer and the content check is a backstop — useful layering, not redundancy.

## 3. arc52 section scoping

`§3 / §5 / §7 / §10` covered the architecture story cleanly, **with one note about §7**.

- **§3 Context & Scope** — clean. The C4Context diagram plus a short "out of scope" list answers "what is this and what touches it?" without leaking into building-block detail.
- **§5 Building Block View** — clean. L2 + L3 are both needed and earn their place; see §4. The 8-tier tier map from `architecture/system.md` was re-cast as the L3 Component diagram rather than re-listed; readers who need the tier map can follow the cross-reference to `architecture/system.md`.
- **§7 Deployment View** — **scope corrected during pilot**. The ticket instruction said to pull §7 from `docs/docker.md`, but `docs/docker.md` is top-level workspace documentation, explicitly out of scope per the map. The 03 grilling's §7 example summary (engine containerized, HTTPS on 443) described the workspace topology, not the engine's own contract. The pilot's §7 instead describes the **engine's deployment contract** — process boundary, DB file, FS directories, outbound HTTPS — and explicitly defers workspace-level topology (Caddy, `no-internet` network, sibling AI-stack containers) as out of scope. No discrepancy remains open; the scope error was in the ticket, not the resolution.
- **§10 Quality Requirements** — clean and novel content. No section wanted to become a fifth arc42 heading; the four-section shape carried the architecture story end-to-end.

No section wanted to burst into its own file. Each one is a self-contained "answer to one question" within the overview.

## 4. C4 level fit

**Verdict: L1 + L2 are required; L3 is required for the Component view of the Application Core, but redundant in many other deployments.**

The test was: does L3 earn its place, or is it redundant with L2?

- **L1 (Context)** is required. There is no other diagram that captures "the engine is one system among several external systems." This is the orienting diagram for a new reader.
- **L2 (Container)** is required. The four containers (HTTP server, application core, SQLite, outbound LLM clients) are a stable, durable vocabulary — every ADR and every system doc implicitly refers to them. Without L2 the rest of the architecture story is ungrounded.
- **L3 (Component)** earned its place **inside the Application Core**, where the domain/application/adapters/bootstrap decomposition is load-bearing (it's the dependency-invariant story). Outside the Application Core — for the SQLite container or the HTTP server — L3 would have been redundant: those containers are atomic from the engine's perspective.

So the L3 diagram in the pilot zooms into one container (Application Core). This is the standard C4 usage pattern and the right call. L3 is not "needed at the architecture-overview level" in general; it is needed because the engine has an internal hexagonal layout that the reader needs to see.

If the engine ever grew a second complex container (e.g. a separate scheduler service), that container would warrant its own L3 diagram. Today, one L3 for the application core is enough.

## 5. Naming and organization

The top-level shape that felt natural: `reference/` + `explanation/`, with no `tutorials/` or `how-to/` until the pilot surfaced content for them (none did — see §7).

Within `explanation/`, `architecture/` as a subfolder for the arc42-shaped overview felt right because:

- Architecture is a category, not a single document. Future explanation docs on "Why Hexagonal Architecture?" or "Why the LLM Provider Port?" would naturally sit alongside `architecture/overview.md`.
- The arc42 shape is specific enough that mixing architecture docs with discursive explanation docs at the same level would dilute the signal. `explanation/architecture/` keeps the arc42-shaped docs findable.

Within `reference/`, no subfolders emerged. Each reference doc is small enough (the data-layer schema is ~5 KB rendered) to live flat. If a future reference doc grows large, it can split by topic without disturbing siblings.

What felt forced: nothing structural. The only naming friction was inside the architecture doc — "Building Block View" is arc42 vocabulary that a reader unfamiliar with arc42 will not parse on first read. Acceptable because the arc52 section is identified explicitly in the front-matter and the H2 heading is self-describing in context.

## 6. What old-docs content got dropped

Audit of the source docs and the new pilots:

- **From `system/game_flow.md`:** the entire "Two State Channels" aside (~150 words) was moved to `explanation/two-state-channels.md`. It did not fit Reference — it answered "why two signals?" not "what is the phase sequence?" — so it was a clean drop from the reference file. Nothing was force-fit.
- **From `architecture/system.md`:** the tier map (8-tier breakdown) is not re-listed in the architecture overview. The L3 Component diagram covers the high-level structure (domain / application / adapters / bootstrap), and a reader who wants the exact module path for a tier can follow the cross-reference to `architecture/system.md`. The tier map is reference content; the L3 component view is explanation content; they coexist.
- **From `architecture/rust_technical.md`:** the cross-cutting Rust idioms (sync services, `spawn_blocking`, `Arc<RwLock<AppSettings>>`, poison recovery, `Arc<AtomicBool>`, `CancellationToken`) are **not** re-listed in §10. They are mechanism docs; §10 is a quality-attribute list. The cross-reference at the bottom of the architecture overview and inside §10's source-of-truth column points readers to `architecture/rust_technical.md` for the mechanism. This is a deliberate separation: §10 promises the guarantee; `rust_technical.md` shows how it is delivered.
- **From `docs/docker.md`:** **not pulled into the architecture overview.** `docs/docker.md` is top-level workspace documentation (out of scope per the map), not a Chronicler Engine doc. The pilot's §7 Deployment View describes the engine's own deployment contract (process, DB file, FS directories, outbound calls) and explicitly defers workspace-level topology as out of scope. This corrects a scope error in the ticket, which had said to pull §7 from `docs/docker.md`.
- **From `reference/data_layer.md`:** the per-column tables (one Markdown table per SQL table, listing every column with type and notes) were **dropped**. The column-level DDL is already the authoritative source in `src/adapters/driven/storage/db.rs`; restating it in markdown was drift-prone duplication — the existing doc had 5 tables documented while `db.rs` creates 11, so the drift was already real. The pilot's `data_layer.md` instead carries prose per table (what it's for, the load-bearing invariants) plus a Mermaid `flowchart` of all 11 tables' relationships. **Convention**: Reference docs defer column/type details to source; a relationships diagram is permitted where the aggregate structure isn't obvious from the DDL. This carve-out is recorded in the map's Notes.
- **Nothing was force-fit.** If the source content did not fit the new frame, it was either dropped (no replacement yet) or split (the Two State Channels aside) or cross-referenced (the tier map, the Rust idioms, the broader Docker topology).

## 7. Tutorial / How-to candidates

The pilot **did not surface Tutorial or How-to content that needs to be written now**. The audit predicted this might happen; it did not. Specifically:

- **Tutorial candidates:** none surfaced. A "getting started" walkthrough is plausible (audit recommendation 7.3.2), but it would be net-new content not derivable from the existing docs, and writing it is out of scope for this pilot.
- **How-to candidates:** none surfaced from the pilots. Existing `diagnostics/DEBUGGING.md` is the only current How-to in the workspace; the audit recommendation 7.3.1 (flag mixed-purpose files for mode-split) was the closest actionable signal, and the pilots acted on it (splitting `game_flow.md` and reframing the architecture docs).

If Tutorials are to be written at all, they should be charted as separate tickets, not implied by the pilot.

## 8. Framings that didn't fit cleanly

Three honest frictions:

1. **The §7 scope correction.** The ticket (and the 03 grilling's example summary) described §7 as workspace Docker topology pulled from `docs/docker.md`. That was a scope error: `docs/docker.md` is top-level workspace documentation, explicitly listed out of scope in the map. The pilot's §7 was rewritten to describe **only the engine's deployment contract** — the process boundary and what it binds, reads, and calls out to — and defers workspace-level topology (Caddy, `no-internet` Docker network, sibling AI-stack containers) as out of scope. The architecture doc no longer links to `docs/docker.md`. This corrects the discrepancy cleanly; no current-vs-target split is needed.

2. **The `is_generating` cache versus the per-game registry.** ADR-030 distinguishes three layers — persisted `GenerationStatus`, per-game registry, atomic projection — but the pilot's `two-state-channels.md` describes only two channels (persisted + atomic), with the per-game registry implicit in "the registry claim/release path". This was a deliberate simplification: the two-channel frame is the architectural tradeoff the engine makes; the registry is the mechanism the atomic is a projection of. A reader who needs the full three-layer story can follow the ADR-030 cross-reference. If this confuses readers, the explanation doc can grow a third paragraph distinguishing the registry from the atomic, but the pilot kept it short on the principle that explanation should not bloat into mechanism docs.

3. **The LLM HTTP timeout is reported as 180s in §10.** INV-004 cites the 180s figure. The figure is also restated in ADR-010 ("the LLM transport enforces only a 180-second HTTP timeout"). No friction here, but it is worth noting that §10 cites a number that lives in two places (guardrails INV-004 and ADR-010 §Cooperative cancellation only). If the figure ever changes, both citations need updating. A future ticket could centralize timeout/limit values in one config-style reference; for the pilot, the dual citation is acceptable.

4. **Negative-disclaimer paragraphs about what the diagrams don't show.** The first draft of the §3 architecture doc carried paragraphs saying "SillyTavern is not a runtime system" and "Harper is not an external system". This was defensive framing — it kept the very things it was disclaiming in the reader's mind, and read like apologizing for ticket 03's summary errors (which the reader never saw). **Convention**: the C4 diagrams and the Out-of-scope list are the source of truth for what's external. If something isn't in the diagram, it isn't in scope — no parallel negative paragraphs. Inspirations belong in the explanation doc for the thing they inspired (e.g. SillyTavern's prompt-system inspiration lives in the prompt-system doc, not the architecture overview). Harper appears only in the places where its in-process nature is a positive statement of fact (§5 Adapters description mentions `harper_core`; §7 deployment contract has an "In-process text check" bullet); it is not editorialized about.

5. **Three slop patterns to avoid in bulk-writing** (found in pilot audit, 2026-07-15):
   - **Negative-disclaimer prose** — paragraphs insisting that X is "not an external system" or "not in scope". Diagrams and Out-of-scope lists are the source of truth; if something isn't in the diagram, it isn't in scope. Don't editorialize about absences.
   - **Tables-of-contents-as-prose** — a table that maps the doc's own sections to "Question answered" / "C4 level" / "Source of truth". The sections are coming right up; the reader doesn't need a preview. H2 headings + their prose are the TOC.
   - **Dual citations / vague guarantees** — citing both INV-004 and ADR-010 for one value, or "O(1)" when the guarantee is "no storage round-trip per poll". Cite the single authoritative source; describe the guarantee concretely.
   These apply to all four Diátaxis modes, not just architecture docs.

## 9. Failure modes the validator cannot catch (writer-checklist catalogue)

When writing a Reference doc, the validator (`validate_docs_diataxis.py`) catches **structure** — cross-reference targets exist on disk, front-matter has the required keys, required H2 sections (`## Overview`, `## Document References`) are present, no negative-explaining at the line level, no missing mode declaration, body prose uses backticks for path references. It does not catch **substance** — whether the doc is factually accurate, whether a setting/field/API/call-site matches the source, whether the rationale is "what" vs. "why", whether scope was respected. These require writer discipline. An audit across tickets 09, 10, 11, and 12 surfaced a recurring catalogue worth graduating here for future bulk-writing tickets (13 dashboard/UI, 14 guardrails/tier map, 15 tests).

**Group A — Reference defers to source (failure to defer):**

- **Restating DB tables / SQL column names.** Reference docs that describe a management SYSTEM (not the table doc) must not name SQL tables, columns, FK constraints, or quote SQL queries verbatim. `data_layer.md` is the table doc; everything else describes a domain. *Ticket 12 `worlds.md` first draft named `worlds.key`, `games.world_key`, `maps.world_key`, quoted a verbatim `SELECT COUNT(*) FROM games WHERE world_key = ?` query, and described FK-cascade prose — all replaced with domain phrasing or `./data_layer.md` cross-refs.*
- **Restating JSON Schema fields verbatim.** Even when the doc is JUST about an object, do not copy the schema. Defer with `./data_schemas.md` and let the prose describe the behavior. (Ticket 11.)
- **Asserting constraint details without code verification.** Rules like "unique, alphanumeric plus underscore only" or "non-empty" are easy to assert; they may not be enforced anywhere in the handler, the template, or the SQL. Each constraint should be traceable to one of: a SQL constraint in `db.rs`, a JSON-schema keyword, a handler regex, a template `pattern` attribute, or an explicit test. *Ticket 12 `worlds.md` first draft asserted two such rules; neither was enforced.*

**Group B — Mechanics leaks (code-indexer forms in Reference prose):**

- `Type::method()` Rust syntax (e.g., `WorldForm::into_world_card`, `evaluate_triggers(state)`, `world.default_scenario()`). Replace with descriptive prose.
- Bare impl-detail function names that name the mechanism, not the contract.
- Struct field dumps in prose (e.g., `Message::message`, `Message::sender`, `Message::timestamp`).
- Variant payload / `Option<T>` syntax in non-load-bearing positions (`NarratorFailed(String)`, `PersistFailed { label, source }`).

The discriminator: **does this name something a reader needs to grep for to find the contract?** HTTP handler function names, type/variant names, enum labels — these are seam identifiers and stay. Function-call forms, method-call forms, field lists, payload shapes, SQL columns — these name the mechanism and go. The Mermaid-diagram node-label rule is a special case: keep handler function names (seam identifiers) but rewrite `Type::method()`-form labels into prose.

**Group C — Factual accuracy (writer's pre-flight check):**

- **Fabricated struct field names.** When the source doc doesn't list a struct's fields, do not fill the gap from memory. Defer with prose describing what the field is FOR and pointing at the source. *Ticket 12 `text_check.md` Issue Classification paragraph invented `start`/`end`/`message`/`suggestions` — three of four were wrong; actual struct has `span`, `message`, `suggestion`, `kind`.*
- **Wrong JSON field names.** Flat names when the schema is nested, or names that don't exist. *Ticket 12 `text_check.md` Settings wrote `text_check_mode` and `text_check_ignored_words`; actual schema is `text_check.mode` and `text_check.ignored_words` nested.*
- **Missing load-bearing settings / fields.** When a setting/flag has functional effect (gating, toggling, falling back), describe it. *Ticket 12 `enable_auto_check` setting gates pre-flight at `src/adapters/driving/http/fragments/actions.rs:85`; first-pass doc omitted it entirely.*
- **Wrong call-site / lifecycle.** When a doc says "X happens at Y" (e.g., "validated on write", "loaded once at startup"), verify against source. *Ticket 12 `text_check.md` "engine settings loaded once at startup" — actually mode and enable_auto_check are read per-call; only the dictionary is loaded once at service construction. Ticket 12 `worlds.md` "validated on write" — actually validated on load at `bootstrap/validate.rs:24-30`.*
- **Carrying stale source-doc claims.** Source docs can be wrong. If both the source and the new doc assert X, both may be wrong. Verify at the code level. **A Reference doc that faithfully migrates from a source doc is only as accurate as the source doc.** Source-doc carry-over deserves MORE scrutiny than a green-field claim — the writer's confidence is artificially inflated by source-doc authority (the prose already "sounds right" because it's been reviewed or lived with), but the writer has not personally verified it. The trim of ticket 12 surfaced five instances of this pattern across three docs in a single audit pass:

    - `worlds.md` "alphanumeric plus underscore only" and "non-empty" key/name constraints — neither enforced anywhere; carried verbatim from `docs/system/worlds.md`.
    - `worlds.md` "lore, global rules, scenarios, and a default-room-image fallback" — "lore" isn't a field at all (`WorldCard` has `name`/`description`/`global_rules`/`scenarios`/`default_scenario_id`/`default_room_image`); carried from source's hand-wave prose.
    - `worlds.md` "active scenario's `starting_room_id`" — "active scenario" is invented; the code term is `default_scenario()` returning the first scenario (`src/domain/model/world.rs:46, 56`); carried from source's vague phrasing.
    - `navigation.md` "subsequent narration can attach exits, items, or NPCs" — `Room.exits` is `HashMap::new()` and `Room.items` is `vec![]` at creation (`src/domain/engine/logic.rs:33-44`), and `logic_tests.rs:164` asserts `room.items.is_empty()`; carried from `docs/system/dynamic_rooms.md` (which itself hand-waved the same claim).
    - `text_check.md` "spelling, grammar, capitalization, formatting, and style issues" — Style and Formatting order was swapped and the `Other` variant missing; the source `docs/system/text_check.md` had the same wrong order, both likely carried from an even-older doc.

  Treat every claim in the source doc as a hypothesis to verify, not as ground truth to lift.

The pre-flight check for Group C: **for every substantive claim that names a setting / field / API / call-site / lifecycle / constraint, the writer should be able to cite the file:line it comes from.** If the writer can't, the claim is suspect and should be rephrased to defer, or dropped.

**Group D — Tone discipline (writer's craft choices):**

- **Negative-explaining.** Tautological negatives ("X has no Y field"), defensive scope disclaiming ("X is not an external system"), negative tails ("does not call back"). Rephrase as positive constraints. (Already in `AGENTS.md`'s `## No negative explaining`; tickets 09 and 12.)
- **Scope-disclaiming editorializing.** "The X doc does not coordinate Y" / "this is out of scope here" / "Y is managed elsewhere." Reader doesn't need to know which doc DOESN'T do something; they need to know which doc DOES. *Ticket 12 `worlds.md` Map Cleanup bullet closed with "The cascade is managed in storage; the worlds doc does not need to coordinate it." — dropped.*
- **Justification framing in Reference.** "Why X?" headers or "We chose X because..." prose. Belongs in Explanation; Reference describes what IS. (Ticket 21 register correction; retrofit log §2026-07-15 Explanation-doc register correction.)
- **Dropped rationale without an Explanation home.** When Reference strips the "why" (e.g., during negative-explaining retrofit), it must either explicitly cross-reference an Explanation doc that carries it, or note "rationale cross-referenced to <ADR>". Do not silently drop. (Tickets 09, 20.)
- **Dual citations / vague guarantees.** Citing two sources for one value without explaining the seam; "ensures correctness", "handles edge cases", "O(1)" without specifying the contract. (§8.5; tickets 09, 11.)

**Process discipline — multiple tightening passes:**

One tightening pass catches one failure class and misses others. The class of issue caught depends on what the writer is LOOKING FOR. Ticket 12 ran three passes:

- **Pass 1 — negative-explaining + mechanics leaks.** Caught 5 prose drops, 3 `Type::method()` drops, 2 H3 header rephrasings, Mermaid-node-label rewrites. The validator catches most of this if applied carefully.
- **Pass 2 — DB-mechanics scope.** Caught column-name references, FK-cascade prose, verbatim SQL queries, scope-defense editorializing. The class ticket 11 caught and fixed in its rework.
- **Pass 3 — factual accuracy.** Caught fabricated issue fields, wrong JSON field names, missing settings, wrong call-site claims. The validator structurally cannot catch any of these; only writer pre-flight can.

**Minimum four-pass discipline per future Reference-writing ticket:**

1. **Pass 1 (validator-friendly).** Negative-explaining, mechanics leaks, Mermaid-node labels, vague references, body-prose backtick discipline. Validator catches most.
2. **Pass 2 (Group A deferrals).** DB-table scope, JSON-Schema deferral, constraint-traceability, no SQL in non-table-management docs.
3. **Pass 3 (Group C claim-vs-source).** Every substantive claim in the doc maps to a specific file:line in `src/` or `data/schemas/`. If you can't name the source, rephrase or drop.
4. **Pass 4 (Group D tone + scope).** No scope-disclaiming, no justification framing, dropped rationale cross-referenced, no dual citations, no vague guarantees, mode-appropriate register (Reference = what IS; Explanation = why/design tradeoff).

The **validator is necessary but not sufficient**. A clean `validate_docs_diataxis.py` (default OR `--strict`) confirms structure; it does not confirm substance.

## 10. `game_flow.md` split verdict

**The Two State Channels split held.** The reference doc reads as a clean phase/phase-table/retry/error-model contract; the explanation doc reads as a coherent essay on the dual-channel tradeoff. No content from the original aside had to be force-fit into either file, and no new content had to be invented to bridge them.

What the split revealed:

- The reference doc's "Error Model" section has a one-line cross-reference back to `explanation/two-state-channels.md` at the end of "Granular Status Phases". Without that cross-reference, a reader of the reference doc would be left wondering why there are two signals. The cross-reference is essential — it is the seam between the two docs.
- The reference doc's "Stale-Generating recovery" sentence still names the atomic flag and the persisted status by their concrete shapes. This is borderline — it is factual description of what happens, not "why". Acceptable because it describes observable behavior of the engine; the "why" lives in the explanation doc.
- The retry-flow diagram and status-phase table are pure Reference and stayed where they were.

If a future ticket introduces a third channel (e.g. for cross-process coordination), the explanation doc will need a small extension but the reference doc will not. The split ages well.

## Summary

The pattern works. Front-matter is justified; the architecture doc's arc52 shape carried the story; L3 was earned by the application-core decomposition but not needed elsewhere; the Two State Channels split held; no Tutorial or How-to content surfaced from the pilots; the deployment story needs a human decision.

Outstanding for human review: none. The §7 scope error was corrected during the pilot; the two minor judgment calls (two-channels vs three-layers in `two-state-channels.md`; dual 180s timeout citation) are noted above and do not block charting the bulk-writing tickets.

## Retrofit log

This section records when already-shipped pilot docs are retrofitted under a convention added after they shipped. New entries append here; the originating convention is named so a reader can trace the deviation.

- **2026-07-15 — Negative-explaining retrofit (ticket 09 review).** During ticket 09 review, the user identified two instances where the shipped docs described a thing by what it wasn't rather than what it was (the `message_model.md` "Swipe is the Sole Holder of Content Fields" tautological-negative section, and the `agent_system.md` "Quantifier Forensics Gap" defensive-absence callout). The convention was promoted to `AGENTS.md` as a new `## No negative explaining` section (two banned forms: tautological negative definition; defensive scope disclaiming; escape hatch via positive constraint). A full sweep of `docs-diataxis/` retrofitted the pilot docs under the new convention: `narration_engine.md` (drop the `## Game Master Boundaries` "must not" block + the Overview's "does not mutate / cannot call back" clauses), `two-state-channels.md` (drop the `## What this design does not address` section and the `## What this forbids` "writing it is not" clause), `action_pipeline.md` (drop "retry logic is not a duplicate" / "pipeline is not an event-driven" tails), `message_model.md` (drop "there is no public `set_text`" / "Callers cannot bypass with `.push()`" tails), `architecture/overview.md` (drop §7 harper "No separate service" tail + five §10 table-cell defensive-absence tails). Validator re-checked PASS on both default and `--strict`.
- **2026-07-15 — chronicler-docs-hygiene retrofit (full-tree audit).** User directed the `chronicler-docs-hygiene` skill against the full `docs-diataxis/` tree (excluding `_PILOT_NOTES.md`, which is temporary). The audit found the same three failure modes in committed pilot docs that ticket 09 had surfaced in its own docs, plus mechanics leaks and cross-doc drift. Findings and fixes by phase: **Phase 1 sediment** in `AGENTS.md` — dropped ticket-ref annotations on the three-layer enforcement table (`extended by ticket 04/05`), the past-tense audit narrative ("the audit found zero Tutorials and only one How-to (`DEBUGGING.md`)"), and the future speculation about ticket 06's bulk-writing survey; trimmed the `Tutorial`/`How-to` mode-specific notes to durable definitions only; dropped the "Drift was already real" past-tense justification; trimmed "(refined during pilot review)" provenance from the no-code-indexer carve-out cell. **Phase 2 mechanics leak** — `data_layer.md` Migrations section dropped the `run_migrations()` function name (kept the `src/adapters/driven/storage/db.rs` deferral pointer); `game_flow.md` Error Model dropped the `status.error_message()` call syntax; `narration_engine.md` Game Master Context dropped the `PromptAssembler::assemble()` type::method reference; `message_model.md` Persistence Notes dropped `Storage::purge_soft_deleted` method path. **Phase 4 code-indexer in tables** — `action_pipeline.md` PhaseError table cells dropped variant payload-shape syntax (`NarratorFailed(String)` → `NarratorFailed`; `PersistFailed { label, source }` → `PersistFailed`; `FetchFailed(String)` → `FetchFailed`), keeping the variant name (contract reference) and folding payload semantics into the Contract cell prose; `triggers.md` Mutation Order Invariant table dropped function-call notation (`handle_movement()` / `evaluate_triggers()` / `commit_trigger_narration()` / `apply_npc_events()`) from the Step column, replacing with behavioral descriptions, and added a one-line deferral pointer to `src/domain/engine/`; `agent_system.md` per-action flow dropped `execute_freeaction_impl`. **Phase 6 behavior mismatch** — spot-verified two load-bearing behavioral claims against `src/`: `data_layer.md` "messages are not stored in the snapshot JSON" holds (snapshot uses `NarrativeSnapshot` sans messages, confirmed by rustdoc `src/domain/model/state/game_state_snapshot.rs:20`: "[`NarrativeState`] sans messages (separate table, hydrated on load)"); `triggers.md` `times_met` semantics match `apply_npc_events` in `src/domain/engine/action_processing.rs` (`NpcTransitionType::Entered` → `set_currently_meeting(true)` + `increment_times_met`; `NpcTransitionType::Left` → `set_currently_meeting(false)`). **Phase 7 cross-doc drift** — `game_flow.md` Document References cited five old-tree `system/` paths; updated the two that now have new-tree siblings (`system/triggers.md` → `./triggers.md`; `system/narration_engine.md` → `./narration_engine.md`); dropped the three forward-refs to not-yet-written docs (`system/prompt_system.md`, `system/llm_processing.md`, `system/dashboard.md`) to avoid broken links — those cross-refs will be added when tickets 10/13 land. **Tone (light)** — `triggers.md` "Introduction triggers should always carry a `room_id`" prescription → present-tense contract "Introduction triggers carry a `room_id`"; `agent_system.md` `UseMain` defensive future-speculation line → present-tense contract "The `UseMain` selector exists in the enum; no agent currently selects it." Validator re-checked PASS on both default and `--strict` across 11 files.
- **2026-07-16 — Writer-checklist failure modes graduated (tickets 09–12 audit).** A new `## 9. Failure modes the validator cannot catch (writer-checklist catalogue)` was added to `_PILOT_NOTES.md` after ticket 12's three-pass tightening surfaced ten failure modes the validator cannot catch. The catalogue is organized into four groups (A: Reference defers-to-source — DB tables, JSON Schema, constraint-traceability; B: mechanics leaks — `Type::method()`, bare function names, struct field dumps, variant payload syntax; C: factual accuracy — fabricated struct fields, wrong JSON field names, missing settings, wrong call-site/lifecycle, stale source-doc carry-over; D: tone discipline — negative-explaining, scope-disclaiming editorializing, justification framing, dropped rationale without Explanation home, dual citations, vague guarantees). Each group carries ticket-09/10/11/12 instance examples where they exist. **Process discipline** is the closing point: minimum four passes per Reference-writing ticket (Pass 1 validator-friendly; Pass 2 Group A deferrals; Pass 3 Group C claim-vs-source — every substantive claim must trace to a file:line in `src/` or `data/schemas/`; Pass 4 Group D tone + scope). The catalogue is FORWARD-LOOKING — graduated as a pre-writing and post-writing audit checklist for future Reference-writing tickets (13 dashboard/UI, 14 guardrails/tier map, 15 tests). No doc changes this entry; it is a knowledge-graduation entry, not a retrofit. Ticket 12's own corrections are documented in the ticket's `## Resolution` comment and the map's `## Decisions so-far` entry; this entry makes those patterns findable for future writers.
- **2026-07-16 — Source-doc carry-over meta-pattern graduated (ticket-12 trim audit follow-on).** A review of the trimmed `text_check.md`, `navigation.md`, and `worlds.md` surfaced four additional factual-accuracy issues — all matching Group C patterns — revealing a deeper meta-pattern: a Reference doc that faithfully migrates from a source doc is only as accurate as the source doc. Source-doc carry-over deserves MORE scrutiny than a green-field claim because the writer's confidence is artificially inflated by source-doc authority (the prose already "sounds right" because it's been reviewed or lived with), but the writer has not personally verified it. Five ticket-12 instances of source-doc carry-over are now enumerated in the expanded **Carrying stale source-doc claims** bullet under §9 Group C: (1) `worlds.md` "alphanumeric plus underscore only" / "non-empty" key/name constraints — neither enforced anywhere in code; carried verbatim from `docs/system/worlds.md`. (2) `worlds.md` "lore" field — `WorldCard` has no `lore` field (fields are `key`/`name`/`description`/`global_rules`/`scenarios`/`default_scenario_id`/`default_room_image`); carried from the source doc's hand-wave prose. (3) `worlds.md` "active scenario's `starting_room_id`" — "active scenario" is invented; the code term is `default_scenario()` returning the first scenario (`src/domain/model/world.rs:46, 56`); carried from source doc's vague phrasing. (4) `navigation.md` "subsequent narration can attach exits, items, or NPCs" — `Room.exits` is `HashMap::new()` and `Room.items` is `vec![]` at creation (`src/domain/engine/logic.rs:33-44`), and `logic_tests.rs:164` asserts `room.items.is_empty()`; carried from `docs/system/dynamic_rooms.md` (which itself hand-waved the same claim). (5) `text_check.md` "spelling, grammar, capitalization, formatting, and style issues" — `Style` and `Formatting` order was swapped vs the `IssueKind` enum and the `Other` variant was missing; source `docs/system/text_check.md` had the same wrong order, both likely carried from an even-older doc. The bullet now closes with: "Treat every claim in the source doc as a hypothesis to verify, not as ground truth to lift." Pass 3 of the four-pass discipline (claim-vs-source) defaults to verification against source code in `src/` or `data/schemas/`, NOT against source docs.
- **2026-07-15 — Explanation-doc register correction (canonical Diátaxis alignment, ticket 09 follow-on).** After the hygiene retrofit, the user pushed on whether the five ticket-09 Reference docs were genuinely Reference per canonical Diátaxis (diataxis.fr/reference/, diataxis.fr/explanation/). Audit found three docs had clear Explanation drift (rationale, tradeoffs, alternatives-considered) inlined in Reference prose: `agent_system.md` (Overview "Agents exist to make pipeline orchestration pluggable…"; Trait Contract "trait objects rather than generics so…"; Per-Agent Backends "enables cost optimization…"), `message_model.md` (Retry and Swipe Behaviour "Swiping is limited to the last message because…"), plus mild tails in `narration_engine.md` (Game Master Boundaries prompt-preset content), `action_pipeline.md` ("The race is bounded by design"), `triggers.md` (counterfactual + "rather than from static map config"). User correctly rejected leaning on ADRs as the Explanation home — ADRs are frozen decision records (possibly Superseded, possibly deleted per the ADR README's Deletions section), not living current-understanding docs. User chose **Option A (strict canonical Diátaxis): strip rationale from Reference docs, create new Explanation docs that carry the why, Reference links to Explanation.** Pre-flight code verification (task 3) grounded every claim against `src/`: `dyn Agent` at `registry.rs:17`; only `PostGeneration` dispatched at `pipeline.rs:260`; separate `narration_connection_id` + `quantifier_connection_id` at `settings.rs:133-134` with separate recorders per connection at `wiring.rs:30-44`; `BackendSelector` enum at `agent.rs:20-23` recorded but not consulted at dispatch; last-message-only enforced at `message_editing.rs:48-51` ("Only the last message can be swiped"); retry truncates after anchor at `retry.rs:91`; "Never act on behalf of the player" is **prompt-preset content** at `data/prompt_presets/system/default.json`, NOT engine-enforced — so it must be dropped from `narration_engine.md` Reference; increment-order invariant is explicit as a code comment at `action_processing.rs:172`. Two new Explanation docs written: `explanation/agent_system_design.md` (pluggability rationale, trait-objects-vs-generics, per-agent backend, two-phase split) and `explanation/message_swipe_model.md` (why swipes exist, per-swipe snapshot_id, why last-message-only, why narration/event swipes are separate). **Register correction:** the first draft of both Explanation docs was written in a TED-talk / essay register ("A reader opening X is confronted with…", "The fair question is…", "What this design is not", "the perspective the design takes", "a hypothetical prose guardian tomorrow") — narrating the reader's experience rather than explaining the design. User pushed back; the canonical Diátaxis site's example sentences ("The reason for x is because historically, y…", "W is better than z, because…") were taken as the register target. Both docs were rewritten in a flat direct register: design choice, reason, tradeoff, stated without rhetorical performance. Convention was promoted to `AGENTS.md` — `### Explanation` section now carries a `#### Register` subsection listing four forbidden registers (narrated reader experience; dramatic contrast framing; editorializing perspective; speculative color) so future Explanation doc writers hold the line. Still pending in subsequent tasks: strip the actual rationale from the five Reference docs (tasks 6, 7, 8), update AGENTS.md mode notes for canonical alignment (task 9), final validation + ticket-09 scope update (task 10).
